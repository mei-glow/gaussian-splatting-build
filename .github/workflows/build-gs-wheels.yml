name: Build Gaussian Splatting Wheels

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    container: ubuntu:20.04

    steps:
      - name: Install base tools in Ubuntu 20.04 container
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          apt-get update
          apt-get install -y \
            ca-certificates \
            curl \
            wget \
            git \
            gnupg \
            build-essential \
            ninja-build \
            bzip2

      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Miniconda with Python 3.11
        run: |
          wget https://repo.anaconda.com/miniconda/Miniconda3-py311_24.5.0-0-Linux-x86_64.sh -O /tmp/miniconda.sh
          bash /tmp/miniconda.sh -b -p /opt/conda
          /opt/conda/bin/conda --version
          /opt/conda/bin/python --version

      - name: Install CUDA 12.1 in Ubuntu 20.04 container
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          wget https://developer.download.nvidia.com/compute/cuda/repos/ubuntu2004/x86_64/cuda-keyring_1.1-1_all.deb
          dpkg -i cuda-keyring_1.1-1_all.deb
          apt-get update
          apt-get install -y cuda-nvcc-12-1 cuda-cudart-dev-12-1 cuda-command-line-tools-12-1 cuda-compiler-12-1

      - name: Install Python build deps
        run: |
          export PATH=/opt/conda/bin:$PATH
          python --version
          python -m pip install --upgrade pip setuptools wheel
          pip install "numpy<2" ninja packaging
          pip install torch==2.2.2+cu121 torchvision==0.17.2+cu121 --index-url https://download.pytorch.org/whl/cu121
          python - <<'PY'
          import torch
          print("torch:", torch.__version__)
          print("torch cuda:", torch.version.cuda)
          print("torch file:", torch.__file__)
          PY

      - name: Build wheels with setup.py
        env:
          CUDA_HOME: /usr/local/cuda-12.1
          CUDA_PATH: /usr/local/cuda-12.1
          TORCH_CUDA_ARCH_LIST: "7.0;7.5;8.0;8.6;8.9"
          FORCE_CUDA: "1"
        run: |
          set -eux

          export PATH=/opt/conda/bin:/usr/local/cuda-12.1/bin:$PATH
          export LD_LIBRARY_PATH=/usr/local/cuda-12.1/lib64:${LD_LIBRARY_PATH:-}
              
          mkdir -p wheels

          echo "=== GLIBC ==="
          ldd --version

          echo "=== Python ==="
          which python
          python --version

          echo "=== nvcc ==="
          which nvcc
          nvcc --version

          echo "=== torch check ==="
          python - <<'PY'
          import torch
          print("torch:", torch.__version__)
          print("torch cuda:", torch.version.cuda)
          print("torch file:", torch.__file__)
          PY

          echo "=== Build simple-knn ==="
          cd submodules/simple-knn
          python setup.py bdist_wheel
          cp dist/*.whl ../../wheels/
          cd ../..

          echo "=== Build diff-gaussian-rasterization ==="
          cd submodules/diff-gaussian-rasterization
          python setup.py bdist_wheel
          cp dist/*.whl ../../wheels/
          cd ../..

          echo "=== Build fused-ssim ==="
          cd submodules/fused-ssim
          python setup.py bdist_wheel
          cp dist/*.whl ../../wheels/
          cd ../..

          echo "=== Built wheels ==="
          ls -lh wheels

      - name: Upload wheels artifact
        uses: actions/upload-artifact@v4
        with:
          name: gs-wheels-py311-torch222-cu121-glibc231
          path: wheels/*.whl
